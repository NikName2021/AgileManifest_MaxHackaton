# МаксХак — Ассистент сезонного найма (трек «Эффективный бизнес»)

Чат-бот в MAX для небольшого сезонного/локального бизнеса: помогает быстро составить вакансию, показывает рыночный бенчмарк по данным «Работы России», публикует карточку вакансии с кнопкой «Откликнуться» и ведёт простую воронку найма прямо в чате.

Полное описание проблемы, аудитории, архитектуры, модели данных, обоих API (собственного и внешнего) и плана разработки — в [`docs/ТЗ — Ассистент сезонного найма (MAX).md`](docs/ТЗ%20—%20Ассистент%20сезонного%20найма%20(MAX).md). Там же в `docs/` лежат разбор кейса и сравнение идей, из которых эта выбрана.

## Структура репозитория

```
.
├── backend/          — бэкенд (Node.js + TypeScript + Fastify + Prisma), см. ветку backend
├── docker-compose.yaml — Postgres + backend одной командой
├── docs/             — кейс, ТЗ, разбор идей
└── README.md
```

## Быстрый старт (локальная разработка)

```
git clone <ссылка на репозиторий>
cd МаксХак
cd backend
cp .env.example .env   # вписать MAX_BOT_TOKEN (выдаётся организаторами хакатона)
npm install
cd ..
docker compose up -d db
cd backend
npm run prisma:migrate
npm run dev
```

Сервер поднимется на `http://localhost:3000`, в логе должно появиться `connected to MAX as bot`.

### Про сертификат `backend/russian_ca_bundle.pem`

Это не секрет — публичный сертификат «Минцифры» (Russian Trusted Root CA + Sub CA, gosuslugi.ru/crt), без него Node.js на Windows не может установить HTTPS-соединение с `platform-api2.max.ru`. Файл специально закоммичен в репозиторий и подключается автоматически через `NODE_EXTRA_CA_CERTS` в npm-скриптах (`backend/package.json`) — ничего дополнительно настраивать не нужно.

### Полный стек в Docker

```
docker compose up --build
```

Поднимет и Postgres, и backend одной командой (сборка ≤5 минут — требование кейса).

## Статус

- [x] Скелет backend: Fastify-сервер, подключение к MAX Bot API подтверждено (`/me`)
- [x] Схема БД (Prisma): users, vacancies, applications, benchmark_cache
- [x] Docker-compose для Postgres + backend
- [ ] Приём вебхуков от MAX (эндпоинт есть, живой тест ещё не проводили)
- [ ] Диалог создания вакансии
- [ ] Интеграция с trudvsem.ru (рыночный бенчмарк)
- [ ] Сценарий отклика кандидата и воронка

## Ветки

- `master` — общие материалы проекта (этот README, кейс, ТЗ, docker-compose)
- `backend` — backend-код
